<!DOCTYPE html>
<html lang="fr"  data-bs-theme="dark">
  <head>
    <meta charset="UTF-8">
    <title>{% block title %}Titre par défaut{% endblock %}</title>
      <link rel="preconnect" href="https://fonts.googleapis.com">
      <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
      <link href="https://fonts.googleapis.com/css2?family=Open+Sans:ital,wght@0,300..800;1,300..800&display=swap" rel="stylesheet">
      {% if appCss %}
      <link rel="stylesheet" href="/dist/{{ appCss }}">
      {% endif %}
      {% if appJs %}
      <script type="module" src="/dist/{{ appJs }}"></script>
      {% endif %}
      {% block scripts %}{% endblock %}
      {% block styles %}{% endblock %}
  </head>
  <body>
    <style>
    .front {
      clip-path: inset(0 50% 0 0 round 999px);
      -webkit-clip-path: inset(0 50% 0 0 round 999px);
    }
    </style>
    {% block content %}{% endblock %}
    <footer class="text-center py-3 mt-auto" style="font-size: 0.75rem; color: rgba(255,255,255,0.3);">
      Fait par HelloAsso{% if appVersion %} &mdash; <span title="{{ appVersion }}">{{ appVersion|slice(0, 7) }}</span>{% endif %}
    </footer>
  </body>
</html>